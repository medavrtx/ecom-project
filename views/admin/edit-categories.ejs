<% layout('layouts/boilerplate')%>

<div class="container-fluid">
  <div class="row">
    <!-- Sidebar Navigation -->
    <div class="col-lg-2 bg-light sidebar py-4">
      <ul class="nav flex-column pt-4">
        <li class="nav-item">
          <a class="nav-link link" href="/admin/edit-products">Edit Products</a>
        </li>
        <li class="nav-item">
          <a class="nav-link link active" href="/admin/edit-categories">
            Edit Categories
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link link" href="/admin/best-sellers">Best Sellers</a>
        </li>
      </ul>
    </div>

    <div class="col-lg-10 content">
      <div class="my-5">
        <h1 class="center-title"><%= pageTitle %></h1>
      </div>

      <!-- Add New Category Section -->
      <div class="container-lg mt-4">
        <div class="row mx-5">
          <div class="col-lg-6">
            <h2>Add New Category</h2>
            <form action="/admin/add-category" method="POST">
              <div class="form-group">
                <label for="title"></label>
                <input
                  type="text"
                  class="form-control"
                  id="title"
                  name="title"
                  placeholder="Category name"
                  required
                />
              </div>
              <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
              <button type="submit" class="btn btn-primary">
                Add Category
              </button>
            </form>
          </div>
        </div>
      </div>

      <!-- Edit Categories Section -->
      <div class="container-lg my-5">
        <div class="row mx-5">
          <div class="col-12">
            <% if (categories) { %>
            <div class="row align-items-start">
              <% for (let category of categories) { %>
              <article class="col-lg-4 mb-4">
                <div class="card">
                  <p
                    class="d-block text-dark text-decoration-none py-2 my-0 category-name"
                  >
                    <%= category.title %>
                  </p>
                  <!-- List all products in this category -->
                  <ul class="list-group">
                    <% for (let product of category.products) { %>
                    <li class="list-group-item">
                      <%= product.title %> - $<%= product.price %>
                    </li>
                    <% } %>
                  </ul>
                  <div class="btn-row my-2">
                    <a href="/admin/categories/<%= category._id %>/edit">
                      <button type="button" class="btn btn-outline-dark">
                        Edit Category
                      </button>
                    </a>
                    <input
                      type="hidden"
                      name="categoryId"
                      value="<%= category._id %>"
                    />
                    <input
                      type="hidden"
                      name="_csrf"
                      value="<%= csrfToken %>"
                    />
                    <button
                      class="btn btn-outline-dark"
                      type="button"
                      onclick="deleteCategory(this)"
                    >
                      Delete Category
                    </button>
                  </div>
                </div>
              </article>
              <% } %>
            </div>
            <% } else { %>
            <p>No categories found.</p>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
